spec:
  name: hello
  preCmd: go build -o main cmd/static/main.go
  cmd: ./main
  logdir: stdout
  livenessProbe:
    tcpSocket:
      port: 27000
    initialDelaySeconds: 1.5
    periodSeconds: 0.1
    failureThreshold: 2
    successThreshold: 1